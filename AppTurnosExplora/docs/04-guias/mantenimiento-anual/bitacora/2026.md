# Bitácora de mantenimiento — Año 2026

Fecha de ejecución: 2026-07-24  ·  Responsable: (equipo)

## Estado de partida
- Working tree: con cambios en curso (rama `main`)
- Versión de Python: **3.14.1** (con soporte)
- Versión de Django antes: **5.2.2** (junio 2025)  →  después: **5.2.16**

## Django (seguridad)
- Motivo: la 5.2.2 tenía **más de un año** y le faltaban todos los parches de la serie 5.2
  publicados desde entonces. Referencia revisada:
  https://docs.djangoproject.com/es/6.0/releases/security/
- Acción: `pip install --upgrade "Django>=5.2.16,<5.3"` → instaló **5.2.16** (último parche
  disponible en PyPI de la serie 5.2; verificado con `pip index versions Django`).
- Pins actualizados en:
  - `requirements.txt` (raíz, UTF-16)
  - `AppTurnosExplora/requirements.txt` (UTF-16)
  - `AppTurnosExplora/requirements-dev.txt` (UTF-8)
- CVE más relevante para esta app: **CVE-2026-7666** — transmisión de correo sin cifrar vía
  STARTTLS en el backend SMTP (la app envía correos por SMTP con TLS). Otros CVEs de la serie
  aplicaban a caché/middleware, cookies firmadas y uploads.

## Validación
- `manage.py check` → sin problemas (0 silenciados).
- `manage.py test` → **305 tests, OK (1 skipped)**, ~218 s. Sin regresiones.
- Nota: parche dentro de la misma LTS (5.2.x), sin cambios de API → riesgo bajo, confirmado por tests.

## Decisión sobre versión mayor
- Existe **Django 6.0.x** (última 6.0.7) en PyPI.
- Decisión: **posponer**. La migración 5.2 → 6.0 es un salto de versión mayor (posibles cambios de
  API) y debe hacerse en su propia rama con pruebas dedicadas, no junto al parche de seguridad.

## Pendiente / próximo año
- Volver a correr `pip install --upgrade "Django>=5.2,<5.3"` cuando salga el siguiente parche 5.2.x.
- Considerar `pip audit` para auditar CVEs de todas las dependencias (no se corrió esta vez).
- Evaluar formalmente la migración a la siguiente LTS cuando 5.2 se acerque a su fin de soporte.
